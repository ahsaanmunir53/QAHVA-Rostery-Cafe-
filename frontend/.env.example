# Leave this empty (or delete the file) when the backend serves the frontend —
# which is how this project is deployed. Same-origin /api paths are used then.
#
# Only set it if you host the frontend somewhere separate from the API, e.g.
# VITE_API_URL=https://qahva-api.onrender.com
VITE_API_URL=
