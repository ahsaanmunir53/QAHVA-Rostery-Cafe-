# secrets
.env
.env.*
!.env.example

# dependencies
node_modules/

# builds
dist/
build/
*.tsbuildinfo

# misc
*.log
.DS_Store
Thumbs.db

# runtime data — regenerated from the seeds in server.js on boot.
# Committing these is what made the admin password and login tokens
# revert on every deploy.
backend/data/
